// (Consider an Interface called Strategy
// with an abstract method public String getMove(String playerMove)
// which returns the computer’s move:  “R” “P” or “S”
public interface Strategy
{
    public String getMove(String playerMove);
}
